diff --git a/index.html b/index.html
index b126ff2..d444119 100644
--- a/index.html
+++ b/index.html
@@ -486,8 +486,6 @@
   .toast .dot{width:12px;height:12px;border-radius:50%;border:2px solid var(--toast-fg)}
   .toolset{display:contents}
   .btn .key{font-family:inherit;font-size:10.5px;font-weight:700;letter-spacing:.4px;background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.28);border-radius:6px;padding:2px 7px;margin-left:7px;vertical-align:1px;white-space:nowrap}
-  #toolsToggle{display:none}
-  #toolsToggle svg{transition:transform .2s}
   .about-points kbd{background:rgba(17,20,24,.08);border-radius:5px;padding:1px 7px;font-family:inherit;font-size:12.5px;white-space:nowrap}
   .keys-table{display:flex;flex-direction:column;margin-top:2px}
   .keys-table .krow{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:8px 2px;border-bottom:1px solid rgba(17,20,24,.08)}
@@ -520,9 +518,6 @@
     .btn.primary .lbl{display:inline}
     .btn .key{display:none}
     .logo-badge{display:none}
-    #toolsToggle{display:inline-flex;margin-left:auto}
-    header.tools-collapsed .toolset{display:none}
-    header.tools-collapsed #toolsToggle svg{transform:rotate(180deg)}
   }
 </style>
 </head>
@@ -533,8 +528,6 @@
     <div class="logo-name">opencolors</div>
     <button class="logo-badge" id="freePill" title="Why free?">free forever</button>
   </div>
-  <button class="btn icon-only" id="toolsToggle" title="Show or hide tools" aria-expanded="true"></button>
-
   <div class="header-name"><input id="paletteNameInput" type="text" maxlength="40" spellcheck="false" autocomplete="off" aria-label="Palette name" value="Untitled"></div>
   <div class="toolbar">
    <div class="toolset">
@@ -1319,16 +1312,7 @@ $('generateBtn').innerHTML= icon('refresh',18)+'<span class="lbl">Generate<kbd c
 [['sortHue','droplet'],['sortLight','sun'],['reverse','repeat'],['shuffle','shuffle'],['lockToggle','lock']].forEach(([k,ic])=>{ const b=document.querySelector('#dock [data-dock="'+k+'"]'); if(b) b.innerHTML=icon(ic,18); });
 $('undoBtn').innerHTML    = icon('undo',18);
 $('expDice').innerHTML    = icon('refresh',14);
-[['themeBtn','Generation theme'],['newBtn','Create palette'],['checkBtn','Accessibility checks'],['undoBtn','Undo'],['exportBtn','Export palette'],['generateBtn','Generate palette'],['expDice','Random name'],['toolsToggle','Show or hide toolbar']].forEach(([id,label])=>$(id).setAttribute('aria-label',label));
-/* collapsible toolbar (mobile) */
-$('toolsToggle').innerHTML=icon('chevU',16);
-function setToolsCollapsed(c){
-  $('topbar').classList.toggle('tools-collapsed',c);
-  $('toolsToggle').setAttribute('aria-expanded',String(!c));
-  store.set('ocToolsCollapsed',c?'1':'0');
-}
-setToolsCollapsed(store.get('ocToolsCollapsed')==='1');
-$('toolsToggle').addEventListener('click',()=>setToolsCollapsed(!$('topbar').classList.contains('tools-collapsed')));
+[['themeBtn','Generation theme'],['newBtn','Create palette'],['checkBtn','Accessibility checks'],['undoBtn','Undo'],['exportBtn','Export palette'],['generateBtn','Generate palette'],['expDice','Random name']].forEach(([id,label])=>$(id).setAttribute('aria-label',label));
 const MENU_ICONS={image:'image',mood:'sparkles',explore:'grid',random:'refresh',contrast:'sun',vision:'eye'};
 document.querySelectorAll('#newMenu [data-new],#checkMenu [data-check]').forEach(b=>{
   const k=b.dataset.new||b.dataset.check||b.dataset.more;
diff --git a/dev/audit.js b/dev/audit.js
index 7f6a6ab..df1bfcd 100644
--- a/dev/audit.js
+++ b/dev/audit.js
@@ -136,7 +136,6 @@ t('adder visible on touch (hover:none)', /hover\s*:\s*none[\s\S]{0,200}\.adder/.
 t('controls always visible on mobile (max-width)', /max-width:720px[\s\S]*?\.actions button\{[^}]*opacity:\.95/.test(css) && /max-width:720px[\s\S]*?\.adder button\{[^}]*opacity:1/.test(css) && /max-width:720px[\s\S]*?\.swapper button\{[^}]*opacity:1/.test(css));
 t('lang attribute', /<html lang=/.test(html));
 t('viewport meta', /name="viewport"/.test(html));
-t('mobile toolbar toggle exists', /id="toolsToggle"/.test(html));
 t('swap icon rotates on mobile (vertical strips)', /max-width:720px[\s\S]*?\.swapper button svg\{[^}]*rotate\(90deg\)/.test(css));
 t('hex bubble clamped to slice', /container-type:inline-size/.test(html) && /@container \(max-width:80px\)/.test(html));
 
diff --git a/dev/domtest.js b/dev/domtest.js
index 76d62d6..26c0f66 100644
--- a/dev/domtest.js
+++ b/dev/domtest.js
@@ -210,8 +210,6 @@ const SESSION = [
 
   /* header chrome */
   ['name-dice', async (c) => c.click(c.d.getElementById('nameDice'))],
-  ['toolbar-collapse', async (c) => c.click(c.d.getElementById('toolsToggle'))],
-  ['toolbar-expand', async (c) => c.click(c.d.getElementById('toolsToggle'))],
 ];
 
 /* ---------- runner ---------- */
@@ -297,7 +295,7 @@ async function runSession() {
       /* The ink decision per strip. Storing the whole style attribute made the golden
        * ~880 bytes a step and drowned real diffs, so this keeps the part a human needs
        * (which --on won) plus a short hash of the rest: any change to --hover or --sh
-       * still shows up, without spelling it out 98 times. */
+       * still shows up, without spelling it out 96 times. */
       inks: [...d.querySelectorAll('.strip')].map((e) => {
         const st = e.getAttribute('style') || '';
         const on = (st.match(/--on:([^;]+)/) || [, '?'])[1];
diff --git a/README.md b/README.md
index 044c604..150cff8 100644
--- a/README.md
+++ b/README.md
@@ -13,7 +13,7 @@ A free-forever palette maker in a **single HTML file**: no accounts, no paywall,
 - Contrast checker (WCAG), color-vision simulator, full edit panel with shades
 - Save palettes locally, export link / CSS / Tailwind / SCSS / JSON / SVG / gradient / PNG
 - Keyboard-first: `Space` generate · `L` lock · `←` `→` reorder · `Ctrl Z` undo
-- Mobile-friendly with collapsible toolbar
+- Mobile-friendly stacked layout with always-visible controls
 
 ## Quick start
 
@@ -41,8 +41,8 @@ Three node tools, each covering what the others cannot:
 
 ```sh
 node dev/harness.js   #  64 functional smoke tests — app logic against a stub DOM
-node dev/domtest.js   #  98 interaction steps      — real DOM (jsdom), golden snapshot
-node dev/audit.js     #  76 contrast / a11y / heuristic checks
+node dev/domtest.js   #  96 interaction steps      — real DOM (jsdom), golden snapshot
+node dev/audit.js     #  75 contrast / a11y / heuristic checks
 ```
 
 `harness.js` and `audit.js` need nothing but node. `domtest.js` needs jsdom
