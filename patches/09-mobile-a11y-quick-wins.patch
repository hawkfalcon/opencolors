diff --git a/index.html b/index.html
index e99771f..e6fd704 100644
--- a/index.html
+++ b/index.html
@@ -196,7 +196,7 @@
   .hex:active{transform:scale(.95)}
   .strip,.strip button{-webkit-user-select:none;user-select:none}
   .cname{
-    color:var(--on,#fff);opacity:.85;font-size:12.5px;font-weight:600;text-shadow:0 1px 4px var(--sh,transparent);
+    color:var(--on,#fff);opacity:1;font-size:12.5px;font-weight:600;text-shadow:0 1px 4px var(--sh,transparent);
     letter-spacing:.2px;max-width:100%;text-align:center;line-height:1.35;
     white-space:normal;
   }
@@ -513,6 +513,8 @@
     .toolset .btn .lbl{display:inline;font-size:11px}
     .toolset .btn .count{position:absolute;top:5px;right:8px;min-width:18px;height:18px;font-size:10px}
     .menu{max-height:70vh;overflow:auto}
+    /* 16px text inputs: iOS Safari auto-zooms anything smaller on focus */
+    #paletteNameInput,.word-row input,.exp-row input{font-size:16px}
     /* narrow phones scroll the labeled bar instead of squeezing it */
     #dock{max-width:calc(100vw - 24px);overflow-x:auto;scrollbar-width:none}
     #dock::-webkit-scrollbar{display:none}
@@ -753,7 +755,7 @@
   </div>
 </div>
 
-<div id="toasts"></div>
+<div id="toasts" role="status"></div>
 <noscript><p style="padding:20px">This app needs JavaScript enabled.</p></noscript>
 
 <script>
diff --git a/dev/audit.js b/dev/audit.js
index b9fcd91..4a12c48 100644
--- a/dev/audit.js
+++ b/dev/audit.js
@@ -174,6 +174,9 @@ t('editing stretches column', /\.strip\.editing\{flex-grow:2\.6\}/.test(html));
 t('inline edit keeps + cancels', /function inlineDone/.test(html) && /function inlineCancel/.test(html));
 t('no duplicative copy button', !/title="Copy hex"/.test(html));
 t('mobile tabs show labels', /max-width:720px[\s\S]*?\.toolset \.btn \.lbl\{[^}]*display:inline/.test(css));
+t('mobile text inputs dodge iOS auto-zoom', /#paletteNameInput,\.word-row input,\.exp-row input\{font-size:16px\}/.test(css));
+t('toasts announced to screen readers', /id="toasts" role="status"/.test(html));
+t('color names at full opacity', /\.cname\{[^}]*opacity:1;/.test(css));
 t('dock buttons labeled everywhere', /'lockToggle','lock','Lock'/.test(html) && /#dock button \.lbl\{display:inline/.test(css) && !/#dock button \.lbl\{display:none\}/.test(css));
 t('editing siblings keep size on mobile', /max-width:720px[\s\S]*?main\.editing \.strip:not\(\.editing\)\{[^}]*flex:none/.test(css));
 t('edit panel exits via Done only', !/ep-head/.test(html) && !/ep-x/.test(html) && /data-pact="done"/.test(html));
diff --git a/README.md b/README.md
index 63f0d1e..d16560a 100644
--- a/README.md
+++ b/README.md
@@ -42,7 +42,7 @@ Three node tools, each covering what the others cannot:
 ```sh
 node dev/harness.js   #  64 functional smoke tests — app logic against a stub DOM
 node dev/domtest.js   #  96 interaction steps      — real DOM (jsdom), golden snapshot
-node dev/audit.js     #  80 contrast / a11y / heuristic checks
+node dev/audit.js     #  83 contrast / a11y / heuristic checks
 ```
 
 `harness.js` and `audit.js` need nothing but node. `domtest.js` needs jsdom
